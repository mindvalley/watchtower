{
  "name": "watchtower-engine",
  "version": "0.1.0",
  "description": "Scans a repository and scores it against the Agentic Compatibility Benchmark. Headless: never compiles or runs the code it measures.",
  "private": true,
  "license": "UNLICENSED",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "node scripts/build-bundles.js",
    "test": "node --test tests/*.test.js tests/benchmark/*.test.js"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.45.0",
    "js-yaml": "^4.3.2"
  }
}
