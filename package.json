{
  "name": "agent-watchtower",
  "version": "1.0.0",
  "description": "Agent Watchtower — Agentic Compatibility Dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "migrate": "node scripts/migrate.js",
    "migrate:mock": "node scripts/migrate.js --mock",
    "test": "node --test tests/*.test.js tests/db/*.test.js",
    "check:engine": "node scripts/check-engine.js"
  },
  "dependencies": {
    "express": "^4.18.0",
    "jose": "^6.2.4",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "supertest": "^6.3.4"
  },
  "engines": {
    "node": ">=22"
  }
}
